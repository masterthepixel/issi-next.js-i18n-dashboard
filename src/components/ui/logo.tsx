"use client";
import React from "react";

interface LogoProps {
  locale?: string;
}

// Floating ISSI logo removed — component preserved as a no-op so imports remain valid.
export default function Logo(_props: LogoProps): React.JSX.Element {
  return <></>;
}
